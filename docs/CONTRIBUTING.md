# Contributing

Keep changes focused and preserve published URLs.

Before submitting changes:

```bash
bundle exec jekyll build
git diff --check
```

Every post must include `title`, `description`, `lang`, and its original date. Use `tr-TR` or `en-US`. Do not rename published post files without a redirect and explicit SEO review.
